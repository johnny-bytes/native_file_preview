<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App's internal files directory -->
    <files-path name="files" path="." />

    <!-- App's internal cache directory -->
    <cache-path name="cache" path="." />

    <!-- App's external files directory -->
    <external-files-path name="external_files_path" path="." />

    <!-- App's external cache directory -->
    <external-cache-path name="external_cache" path="." />

    <!-- External storage root -->
    <external-path name="external_storage" path="." />

    <!-- Common external storage directories -->
    <external-path name="downloads" path="Download" />
    <external-path name="documents" path="Documents" />
    <external-path name="pictures" path="Pictures" />
    <external-path name="movies" path="Movies" />
    <external-path name="music" path="Music" />
    <external-path name="dcim" path="DCIM" />

    <!-- Root path for maximum compatibility (use with caution) -->
    <root-path name="root" path="." />
</paths> 